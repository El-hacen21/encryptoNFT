import { ContractKeypair } from './types';
export declare const decrypt: (keypair: ContractKeypair, ciphertext: string | Uint8Array) => bigint;
export declare const decryptAddress: (keypair: ContractKeypair, ciphertext: string | Uint8Array) => string;
